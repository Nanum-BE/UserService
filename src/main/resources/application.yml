server:
  port: 9010

spring:
  application:
    name: user-service
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format_sql: 'true'
    show-sql: true

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/user?useSSL=false&allowMultiQueries=true&serverTimezone=UTC
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

token:
  expiration_time: 864000000
  secret: 'iuyweiukfyuewkifuewikfuweikfuewikfuwekifuewik'

